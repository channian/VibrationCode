# 振動監測 Dashboard — 需求書

**版本**：v2（已納入權限、認證、技術棧、匯出與期間比較的決定）
**關聯文件**：`PLAN_agent_platform_refactor.md`、`docs/USAGE.md`
**用途**：作為 Dashboard 設計與實作的依據

---

## 一、目的與定位

Dashboard 是本監測系統的人機介面，服務三件事：

1. **分流** — 讓使用者一眼看出 65 台設備中哪幾台需要注意、有多急
2. **簽核** — 承載異常事件的四階段處理流程（設備工程師 → 工程師主管 → 專家複審 → 結案）
3. **佐證** — 提供判斷所需的趨勢與證據，並呈現 agent 的分析

> **定位提醒**：本系統做的是**篩選與預警**，不是診斷。介面用字須與此一致——呈現「觀察到的現象」與「建議的下一步」，不呈現故障類型判定。詳見計畫書 §8.2。

---

## 二、使用者與權限

### 系統權限層級：兩級

| 層級 | 可做的事 |
|------|---------|
| **管理員** | 全部功能，另可設定台帳（ISO 等級、備機標記）、規則門檻、SLA、角色指派 |
| **使用者** | 檢視全部畫面、在簽核鏈上處理事項、匯出資料 |

### 認證：公司 AD

登入走 AD 認證。資料庫的 `app_user` 只存對應與屬性，不存密碼：
`ad_username`（登入識別）、`ad_sid`（不變識別碼，帳號改名仍可對應）。

### ✅ 已定案：簽核鏈的權限

系統的簽核流程有四關：

```
open → 設備工程師回覆 → 工程師主管審核 → 專家複審 → 結案
```

權限層級只有「管理員／使用者」兩級，兩者以**做法 A + 自送禁核**對齊：

- 四關**不做角色管控**，任何「使用者」都能推進任一關，實際由誰處理依組織
  分工自行約束。四關仍完整記錄誰在何時做了什麼。
- **但不得核准自己送出的那一關**（使用者已明確要求）。

這樣不必維護角色指派表，又擋掉最明顯的濫用情境。

**實作要點**：判斷依據是 `finding_status_history` 裡「推進到目前這一關的
人是誰」，而不是 `finding.assigned_to`——後者是指派對象，未必等於實際
送件的人。前一關由 A 推進、A 就不能推進下一關；按鈕在前端要直接
disable 並說明原因，不能只在後端擋（送出後才被拒會讓人白做一次）。

`app_role` / `user_role` 兩張表維持保留但暫不使用——若日後改採做法 B
（各關限定角色），schema 不需要變更。

### ✅ 已定案：AD 認證

採用使用者提供的既有 AD 驗證模組（已在其他正式系統運行），沿用其設計。
核心行為：

- NTLM bind（密碼不經網路明文傳輸），bind 成功後查通訊錄補齊
  displayName / mail / department / title
- **「AD 連不上」與「帳密錯誤」必須分開處理**——前者拋
  `LdapUnavailableError`，後者回 `None`。混在一起會讓現場誤以為自己
  打錯密碼而反覆嘗試
- `AUTH_MOCK=true` 為開發模式（任何非空帳密皆通過），**畫面上必須強制
  顯示「開發模式」提示**，上線前確認為 false
- 密碼絕不落庫、不寫 log、不寫檔，用完即丟
- AD 連線設定一律從 `.env` 讀，不寫死也不進版控

**移植時需要調整的三處**（參考程式的資料模型與本專案不同）：

| 參考程式 | 本專案 | 處理方式 |
|----------|--------|----------|
| `users` 單表，含 `role` 單一欄位 | `app_user` + `user_role` 兩表，角色可帶範圍（廠區/樓層/系統別） | 依做法 A，登入時只需 upsert `app_user`；角色暫不寫入 |
| `lib.audit.write_audit` | `audit_log` 表 | 改接本專案的稽核表，保留「寫不進去也絕不害登入失敗」的行為 |
| session 存記憶體、重啟即登出 | 同 | 沿用。規模到多台伺服器再考慮共享 session |

參考程式對 `upsert_user` 的兩條規則要保留：**不覆蓋既有角色**（管理員在
UI 調整過的權限不該被下次登入蓋回去）、**department 只在第一次建檔時
帶入**（避免 AD 部門異動安靜覆蓋掉人工修正過的值）。

---

## 三、畫面需求

### P0-1　全廠總覽（Fleet Overview）— 首頁

**目的**：三秒內看出今天要注意哪幾台。

**上方統計卡**
- 設備總數 / 運轉中 / 未運轉（備機）
- 未結案 Finding：依嚴重度分（err / warn）
- 待我處理（依登入者角色）
- 資料異常點數（離線 / 品質不良）

**主體：設備清單**

依嚴重度排序（最需要注意的在最上面），每列顯示：

| 欄位 | 說明 |
|------|------|
| 設備名稱 | 含 Building / Floor / System |
| 量測點數 | 1~2 個點 |
| 狀態燈號 | 紅（err）/ 橘（warn）/ 綠（正常）/ 灰（未運轉） |
| ISO Zone | A/B/C/D，**未分級者明確顯示「未分級」而非留白** |
| 主要 Finding | 目前最嚴重的一項，含已開天數 |
| 趨勢 | ↑劣化 / →持平 / ↓改善 |
| 惡化標記 | 處理中但數值持續惡化者需明顯標示 |

**篩選**：廠區、樓層、系統別、設備類型、狀態、是否備機、是否有未結案 Finding

**互動**：點列 → 進入設備詳情

---

### P0-2　設備詳情（Device Detail）

**設備資訊區**
- 台帳：Building / Floor / System / RPM / 設備類型 / 是否備機
- ISO 等級 **與其來源標註**（工程師分類 / 人工覆寫 / 未分級）；若曾觸發 `ISO_CLASS_SUSPECT` 需顯示提醒
- 量測點清單與感測器狀態（含安裝方向資訊、最後更新時間）

**指標趨勢圖**（可切換量測點）

需呈現的指標：
- `velRMS`（主圖，若已分級則疊 ISO Zone 門檻線）
- `velOA` / `accRMS` / `accOA`
- `accCREST` / `accKURT`（衝擊性）
- `accWeightedMeanFreq`（頻譜重心）
- 運轉時數 / 資料完整度（輔助判讀，說明某段落數據是否可信）

要求：
- 時間範圍切換：7 天 / 30 天 / 90 天 / 自訂
- 標註保養事件（紅虛線）與 Finding 觸發時點
- 支援多指標疊圖比較
- **斷線與未運轉的呈現須遵守下節規範**

#### ⚠️ 資料不完整的呈現規範（前端收集會遇到斷線）

前端振動資料會有斷線與資料不齊的情況。系統將每小時標記為四種狀態，
**圖表必須把它們畫成不同的樣子，不可混為一談**：

| `data_status` | 意義 | 圖表呈現 |
|---------------|------|---------|
| `ok` | 資料完整且運轉中 | 正常繪製 |
| `partial` | 有資料但筆數不足 | 繪製但需**視覺弱化**（如淡色/虛線），並可 hover 看完整度 |
| `no_data` | 完全無資料（**感測器斷線**） | **線段必須中斷**，區間以灰底標示「斷線」 |
| `not_running` | 有資料但設備未運轉（**正常狀態**） | 線段中斷，區間以另一種底色標示「未運轉」 |

**三條硬性要求**：

1. **不可跨缺口連線** — 斷線兩端直接連起來會產生視覺上不存在的趨勢，這是最容易誤導判讀的錯誤
2. **不可把缺值畫成 0** — 0 mm/s 與「沒量到」是完全不同的意思
3. **斷線與未運轉須用不同視覺語彙** — 前者是設備異常要處理，後者是正常狀態（尤其備機）

**輔助資訊**：
- 圖表上方或側邊顯示該期間的**資料涵蓋率**（可分析時數 / 總時數）
- 涵蓋率偏低時明確提示「本期間資料不足，趨勢結論僅供參考」
- 缺口區段清單（起訖、時長、類型）可展開檢視

> 同樣的規範適用於週報：涵蓋率不足時，agent 的結論必須標示信心度，
> 不可用不完整的資料下定論。

**該設備的 Finding 清單** — 含已結案者，可展開看簽核歷程

---

### P0-3　Finding 工作台（Findings Workbench）— 簽核鏈核心

**列表模式**

預設顯示「待我處理」（依登入角色決定是哪一關），可切換為全部。

| 欄位 | 說明 |
|------|------|
| 嚴重度 | err / warn |
| 設備 / 量測點 | |
| 問題類型 | `issue_type` 的中文標籤 |
| 發生次數 | `occurrence_count`，第 N 次 |
| 已開天數 | `days_open` |
| 目前階段 | open / 工程師已回覆 / 主管已審 / 專家已複審 |
| 停留時間 | 在目前階段卡多久 |
| 標記 | 惡化 / 逾期 |

**排序邏輯**：惡化 > 逾期 > 嚴重度 > 停留時間

**單一 Finding 詳情**

1. **觸發證據**：規則、指標、當前值 vs 基準值、門檻
2. **能支撐到什麼程度**（`interpretation_limit`）— 明確告知使用者這個證據的解讀邊界
3. **相關指標趨勢圖**，標註觸發時點與基準期
4. **Agent 分析建議**（可重新觸發產生）
5. **歷史類似案例**（RAG 檢索結果，標明為歷史案例）
6. **簽核歷程**：各階段的回覆、時間、人員
7. **動作區**（依角色顯示）：
   - 設備工程師：填寫現場確認結果、處置、送出
   - 主管：審核 / 退回
   - 專家：複審 / **判定需轉專家系統實測** / 結案
   - 任一階段：標記誤報

> **待確認**：退回（打回上一關）是否需要？誤報標記需要哪一層權限？

---

### P0-4　期間比較（A/B Comparison）

**目的**：同一台設備自己跟自己比——選兩段時間，看指標有沒有變化。
最常見的用途是驗證保養有沒有效。

**操作流程**
1. 選設備與量測點
2. 選 A 段時間（如保養前 30 天）與 B 段時間（如保養後 30 天）
3. 選比較基準（見下）
4. 產出比較表與圖，可匯出

**比較內容**：velRMS / velOA / accRMS / accOA / accCREST / accKURT / dispP2P
的中位數、平均、標準差，以及 A→B 的變化率。

#### ⚠️ 「相同負載區間」的前提

先前 `export_vibcurrent.py` 的做法是先依負載欄位分箱，前後共用相同區間，
再比較各箱的指標——確保比的是「相同操作點」而不是「A 段剛好在輕載、
B 段剛好在重載」。這個邏輯有價值，應該保留。

**但它需要有一個負載訊號。** 目前的狀況是：

| 設備類型 | 可用的負載訊號 | 能否做同工況比較 |
|---------|--------------|----------------|
| 有 SCADA 電流對照的設備 | 電流 | ✅ 可以 |
| 變頻設備且有頻率訊號 | 頻率 | ✅ 可以 |
| **定頻且無 SCADA 對照**（多數 AHU） | **無** | ❌ 只能整段比 |

因此介面需要**依設備動態決定**：有負載訊號時提供「相同負載區間」選項並
顯示分箱結果；沒有時明確說明「此設備無負載訊號，以整段資料比較」，
而不是讓使用者以為有做同工況控制。

**另需呈現運轉時數對照**：A/B 兩段的運轉時數若差異很大，指標差異可能來自
用途變化而非設備狀態變化。這點要讓使用者看得到。

---

### P1-1　週報檢視（Weekly Reports）

- 歷史週報清單（依週次）
- 週報內容檢視（即 agent 產出、系統排版的 HTML）
- 當週報告預覽 / 手動觸發重新產生

---

### P2-1　管理視角（Management View）

- 各階段積壓件數與停留時間分佈
- SLA 達成率
- Finding 新增 / 結案趨勢
- 誤報率統計
- 全廠 ISO Zone 分佈變化

---

## 四、資料與更新

| 項目 | 規格 |
|------|------|
| 資料更新頻率 | 每日排程後更新一次；**不需要即時** |
| 資料時效標示 | 每個畫面需顯示資料最後更新時間（比照 HVM 的 `data_age`） |
| 規模 | 65 台設備 / 120 量測點；趨勢圖每點每小時一筆，90 天約 2,160 點 |
| 資料來源 | 統一透過後端 API，Dashboard 不直接讀檔或查原始資料表 |

---

## 五、非功能需求

| 項目 | 需求 |
|------|------|
| 語言 | 繁體中文介面 |
| **技術棧** | **Python**。後端已是 FastAPI，建議 Dashboard 直接掛在同一個 app 上，用 Jinja2 伺服器端渲染 + HTMX 處理互動（見下方說明） |
| 帳號 | **公司 AD 認證**（樣板由使用者提供） |
| 行動裝置 | **不需要**，桌面版即可 |
| **匯出** | **需要**。詳見下方格式說明 |
| 瀏覽器 | 待確認（公司標準？） |
| 通知 | 待確認 — 新 Finding 或逾期時是否需主動通知？email 或站內？ |
| 效能 | 資料量小（65 台 / 120 點），無特殊要求；趨勢圖需能流暢切換時間範圍 |

### 技術棧建議

既然要用 Python，有三個選項：

| 方案 | 適合度 | 說明 |
|------|-------|------|
| **FastAPI + Jinja2 + HTMX**（建議） | ✅ | 後端 API 已是 FastAPI、週報已用 Jinja2，直接沿用同一套。AD 認證接在 FastAPI 的 middleware 最直接。HTMX 讓篩選、簽核這類互動不必寫前端框架 |
| Streamlit | ⚠️ | 開發最快，但多角色權限與 AD 認證整合彆扭，簽核這種需要精確控制的流程不好做 |
| Dash / Plotly | ⚠️ | 圖表強，但整體是為資料視覺化設計的，簽核工作台這類表單流程會綁手綁腳 |

本系統的重心是**簽核工作台**而非互動式圖表探索，因此建議第一個。
圖表用 Plotly 或 ECharts 以靜態方式嵌入即可。

### 匯出需求

| 畫面 | 匯出內容 | 格式 |
|------|---------|------|
| 全廠總覽 | 設備清單與目前狀態 | Excel |
| 設備詳情 | 指標時序原始數值 | Excel / CSV |
| Finding 工作台 | 事項清單（含簽核歷程） | Excel |
| 期間比較 | 比較表與統計 | Excel |
| 週報 | 報告本身 | HTML（已有）/ PDF（待確認是否需要） |

匯出檔案需含產出時間與篩選條件，避免事後看不出這份資料的範圍。

---

## 六、明確不做

- **不做即時監控**（資料本身為每日排程，無即時需求）
- **不做故障類型判定的呈現**（見 §一定位）
- **不做設備控制**（僅檢視與簽核，無任何操作類功能）
- **不做原始波形／頻譜圖檢視**（諧波欄位不使用，raw 需現場量測，屬專家系統職責）

---

## 七、待確認事項彙整

### ✅ 已確定

| 項目 | 決定 |
|------|------|
| 權限層級 | 管理員 / 使用者兩級 |
| 認證 | 公司 AD（樣板待提供） |
| 技術棧 | Python；建議 FastAPI + Jinja2 + HTMX |
| 行動裝置 | 不需要 |
| 資料匯出 | 需要（Excel 為主） |
| 期間比較 | 需要，含相同負載區間比較（有負載訊號的設備） |

### ⬜ 仍待確認

1. **簽核鏈的權限對應** — 選 §二 的做法 A（不做階段管控，但不能核准自己送出的）
   還是做法 B（依角色管控）？**這項影響後端權限檢查與介面按鈕邏輯，優先度最高**
2. **AD 認證樣板** — 你提到可以提供，拿到後才能確定整合方式
3. **主動通知** — 新 Finding 或逾期時是否要通知？email 還是站內？觸發條件為何？
4. **簽核流程細節** — 是否需要「退回上一關」？誤報標記需要哪一層權限？
5. **週報是否需要 PDF** — 目前產出 HTML
6. **瀏覽器版本** — 公司是否有標準或最低支援版本
7. **是否與現有工單系統整合** — Finding 轉工單
8. **部署方式** — 哪台機器、是否容器化、與 API 服務同機或分開

---

## 八、建議的實作優先序

| 優先 | 項目 | 理由 |
|------|------|------|
| **P0** | 全廠總覽、設備詳情、Finding 工作台 | 這三個構成完整的「發現 → 查證 → 處理」閉環，缺一不可用 |
| **P0** | 期間比較 | 驗證保養成效的主要工具，且既有邏輯可沿用（`export_vibcurrent.py` 的同工況分箱） |
| **P1** | 週報檢視、資料匯出 | 週報由後端產出 HTML，Dashboard 只需列表與檢視 |
| **P2** | 管理視角 | 需累積數週資料後才有意義 |

## 九、可沿用的既有實作

| 需求 | 既有程式 | 說明 |
|------|---------|------|
| 期間比較的同工況分箱 | `export_vibcurrent.py` 的 `_maintenance_effect_table()` | 前後共用相同分箱邊界的邏輯已驗證可用，改寫為服務層函式即可 |
| 四指標比較統計 | `compare_maintenance_vibration.py` | velRMS/velOA/accRMS/accOA 的前後比較與變化率 |
| 資料涵蓋率與缺口呈現 | `vibcore/reporting/` | 週報已實作四態涵蓋率與設備面/系統面缺口分列，Dashboard 可直接沿用同一套資料結構 |
| 趨勢圖的斷線處理 | `vibcore/pipeline/aggregate.py` | `data_status` 四態已備妥，圖表據此斷線即可（見 §三之二的呈現規範） |
