# 設備振動健康模型 — 方法說明

**對象**：需要理解、說明或調整本系統的人員（工程師、主管）
**文件版本**：對應 VFDEdgeHealthModel v2

---

## 整體概念

本系統的核心問題是：**「這台設備現在的振動狀態，和它『最健康』的狀態相比，偏離了多少？」**

傳統做法是直接比較振動數值與固定閾值（例如 > 2 mm/s 就警告），但這有個根本缺點：同一台設備在高負載和低負載時，振動本來就不同，無法用同一個閾值判斷。

本系統的解法分兩步：
1. **先找到設備狀態最好的時段**（基準期），把這個時段的振動特徵當作「健康的樣子」
2. **之後每一筆新資料，都和同工況下的健康樣子比較距離**，距離越遠 → 分數越低

---

## 一、資料管線（Phase 1）

### 1.1 三個核心特徵

| 特徵 | 計算方式 | 物理意義 |
|------|---------|---------|
| `Total_vRMS` | `√(velRMS_x² + velRMS_y² + velRMS_z²)` | 三軸振速合力，反映**結構整體振動能量**（軸承磨損、不平衡、共振） |
| `accOA` | 直接量測 | 高頻加速度總量，對**乾摩擦、潤滑劣化**敏感，比振速更早感知軸承早期損傷 |
| `Crest_Factor` | `velPEAK / (velRMS + 1e-6)` | 峰值與均值的比值，反映**衝擊脈衝強度**（滾珠點接觸損傷、齒輪撞擊） |

> **為什麼選這三個？**
> 三個特徵分別感知不同損傷機制：結構振動能量（整體）、高頻摩擦（早期）、衝擊脈衝（點損傷）。
> 合在一起可以涵蓋旋轉機械大部分失效模式。

### 1.2 開機判斷

設備停機時的振動資料是噪音，不應進入模型訓練與評分。

```
有電流資料 → current_A > 1.0 A 才算開機
無電流資料 → Total_vRMS > 0.1 mm/s 才算開機
```

> **可調參數**：`CURRENT_ON_THRESHOLD`（預設 1.0 A）、`VTRMS_ON_THRESHOLD`（預設 0.1 mm/s）
> 若你的設備空載電流很高，需要調高 `CURRENT_ON_THRESHOLD`。

### 1.3 突波過濾

`Crest_Factor > 20` 的列視為感測器瞬間突波，直接丟棄。

> **理由**：真實設備的 CF 極少超過 10，超過 20 幾乎確定是感測器接觸不良或電氣干擾。
> **可調參數**：`CREST_FACTOR_SPIKE_THRESHOLD`（預設 20）

### 1.4 滾動平滑

對三個特徵欄位做 window=3 的滾動中位數平滑，去除量測噪音。

> **為什麼用中位數而不是平均？**
> 中位數不受極端值影響，即使有少數異常點殘留，也不會拉偏整體趨勢。
> **可調參數**：`filters.py` 中 `smooth()` 的 `window` 參數（若噪音很多可調到 5~7）

---

## 二、基準期自動偵測（Phase 2）

### 2.1 為什麼需要基準期

Mahalanobis 距離需要一個「健康參考點」，就是協方差矩陣和均值向量。
沒有基準期，就無從定義「偏離了多少」。

理想的基準期 = 設備剛做完保養、狀態最佳的時段。

### 2.2 穩定性分數

在沒有保養記錄時，用以下方式自動尋找最穩定的 14 天：

```
stability_score = accOA 中位數 × accOA 變異係數（CV = std/mean）
```

**直覺解釋**：
- `accOA 中位數低` → 設備振動本來就小（狀態好）
- `accOA CV 低` → 這段時間振動很穩定，沒有異常波動
- 兩者相乘越小 = 又低又穩 = 最可能是健康狀態

> **為什麼選 accOA 作為穩定性指標？**
> accOA（高頻加速度）對設備狀態最敏感，最早反映劣化，
> 用它來找「最安靜的時段」比用整體振速更精確。

### 2.3 三層篩選

| 層 | 目的 | 條件 |
|----|------|------|
| 第一層 | 從所有滾動窗口取最低分前 10 個 | `stability_score` 排名 |
| 第二層 | 確認三個特徵都穩定，不只是 accOA | `accOA_CV < 0.20`、`vRMS_CV < 0.25`、`CF_CV < 0.30` |
| 第三層 | 輸出供人工確認 | 存成 `output/baseline_candidates/` CSV |

> **若第二層全部落選**：說明整段歷史資料設備都不夠穩定，程式退回第一層結果並印警告。
> 這時建議人工確認保養記錄，在 `device_mapping.csv` 填入 `train_start/train_end`。

> **可調參數**：
> - `BASELINE_WINDOW_DAYS`（預設 14）：資料時間跨度短時可調小
> - `CV_THRESHOLDS`：三個特徵的 CV 門檻，設備本來就振動變化大時可適度放寬

---

## 三、健康評分模型（Phase 3）

### 3.1 為什麼選擇 Mahalanobis 距離

比較常見的方式是 Z-score（幾個標準差）。Mahalanobis 距離是 Z-score 的多維版本，並且考慮了特徵之間的相關性。

**舉例**：
- Total_vRMS 和 accOA 通常是正相關的（振動大，高頻也大）
- 如果只看單一特徵的 Z-score，會重複計算這個相關性
- Mahalanobis 用協方差矩陣「去除相關性」，讓三個特徵真正獨立貢獻

**簡單類比**：
> 假設你用身高和腿長判斷一個人是否異常。腿長和身高高度相關，
> 如果各自獨立判斷，「高個子」會被誤判兩次。
> Mahalanobis 距離只計算「在已知身高的條件下，腿長有沒有異常」，更準確。

### 3.2 電流工況分層（Load Binning）

**問題**：變頻設備的馬達電流隨負載變化，電流越高 → 振動本來就越大。
如果直接比較不同工況的振動，高負載時期一定比低負載時期「看起來更不健康」，即使設備狀態一樣。

**解法**：把電流資料分成 3 層（低/中/高負載），每層獨立訓練一個 Mahalanobis 模型，評分時只和同層的基準比較。

```
低負載 (Bin 0): 電流 8~10 A   → 對應的健康振動基準
中負載 (Bin 1): 電流 10~12 A  → 對應的健康振動基準
高負載 (Bin 2): 電流 12~14 A  → 對應的健康振動基準
```

> **無電流資料時**：退回單一 Bin，不分層，但模型仍可運作，只是無法消除負載影響。
>
> **可調參數**：
> - `DEFAULT_N_BINS`（預設 3）：基準期資料量少時可降為 2
> - `MIN_SAMPLES_PER_BIN`（預設 15）：每層最少要有幾筆資料，不足則自動合併

### 3.3 λ 自動校準（分數錨點）

Mahalanobis 距離本身只是一個「偏離量」，沒有固定的範圍。
需要一個轉換函式把距離對應到 0–100 分。

轉換公式：
```
health_score = 100 × exp(−λ × distance)
```

λ 的校準邏輯：
```
讓基準期第 95 百分位的距離對應 80 分
→ λ = −ln(0.80) / dist_95th
```

**直覺解釋**：
- 基準期內最差的那 5% 資料（最偏離的 5%）給 80 分
- 比基準期更好的資料 → 接近 100 分
- 劣化後距離越大 → 分數指數型下降

> **可調參數**：`SCORE_AT_95TH`（預設 80）
> - 調高（如 90）→ 校準更嚴格，基準期內分數普遍更低
> - 調低（如 70）→ 校準更寬鬆，基準期內分數普遍更高
> - **驗證準則**：基準期平均分數應達到 85 分以上

### 3.4 分數解讀

| 分數範圍 | 警戒等級 | 建議行動 |
|---------|---------|---------|
| ≥ 80 | Normal | 正常監控 |
| 60–79 | Warning | 縮短巡檢頻率，注意趨勢 |
| < 60 | Critical | 安排人工確認，考慮提前保養 |

> 分數本身的**趨勢**比單點數值更重要。
> 連續 3 天從 82 降到 72 比偶爾出現一筆 65 更值得警覺。
> 建議看 `health_score_smooth` 欄位的趨勢，而非 `health_score` 的逐點值。

---

## 四、可調整參數總表

| 參數 | 預設值 | 位置 | 調整時機 |
|------|--------|------|---------|
| `CREST_FACTOR_SPIKE_THRESHOLD` | 20 | settings.py | 感測器突波很多時調高；環境衝擊多時調低 |
| `CURRENT_ON_THRESHOLD` | 1.0 A | settings.py | 設備空載電流高於 1A 時調高 |
| `VTRMS_ON_THRESHOLD` | 0.1 mm/s | settings.py | 無電流資料且設備停機振動偏大時調高 |
| `MERGE_TOLERANCE_MIN` | 5 分鐘 | settings.py | 振動與電流資料時間戳不同步時調高（最多建議 15 分鐘）|
| `BASELINE_WINDOW_DAYS` | 14 天 | settings.py | 資料總長度不足 30 天時可調小 |
| `CV_THRESHOLDS` | 0.20/0.25/0.30 | settings.py | 設備本來就振動不穩定時，可適度放寬到 0.30/0.35/0.40 |
| `DEFAULT_N_BINS` | 3 | settings.py | 電流變化範圍小（< 2A）或資料少時調為 2 |
| `MIN_SAMPLES_PER_BIN` | 15 | settings.py | 基準期資料少時調低（最低建議 10）|
| `SCORE_AT_95TH` | 80 | settings.py | 校準錨點，一般不需調整 |
| `SCORE_SMOOTH_WINDOW` | 5 | settings.py | 分數抖動嚴重時調大（建議 5~15）；要看即時點位時調為 1 |

---

## 五、分數異常排查

執行 `python test_model.py --diagnose` 可印出以下診斷資訊，協助判斷問題。

### 問題一：基準期平均分數 < 85

**診斷**：看診斷報告【3】基準期 CV 值
- 若 CV 超過門檻 → 自動選的基準期本身狀態不佳
- **解法**：在 `device_mapping.csv` 填入 `train_start/train_end`（手動指定保養後的時段）

### 問題二：分數劇烈飄浮（高低差超過 20 分）

**診斷**：看診斷報告【5】各 Bin 分數 std

**原因 A：Bin 邊界跳動**
- 現象：設備電流剛好在兩個 Bin 的邊界，微小電流波動導致 Bin 編號來回跳，兩個 Bin 的基準不同 → 分數跳
- **解法**：把 `DEFAULT_N_BINS` 從 3 改成 2，邊界變少

**原因 B：基準期 CV 太高**
- 現象：診斷【3】顯示 CV 超標
- **解法**：手動指定更穩定的基準期

**原因 C：量測噪音**
- 現象：各 Bin 分數 std 都偏高但無明顯趨勢
- **解法**：增大 `SCORE_SMOOTH_WINDOW`（如改為 10）；看 `health_score_smooth` 欄位

### 問題三：分數與實際設備狀態不符

**診斷**：看診斷報告【4】相關性
- `Health_Score` 應與 `accOA` 呈**負相關**（相關係數 < -0.3）
- 若相關性為正或接近 0 → 模型沒有捕捉到真實劣化訊號

**可能原因**：
- 基準期包含設備狀態不佳的時段（λ 校準基準錯誤）
- 設備在此段時間電流（負載）變化太大，超過分層能力
- 振動感測器安裝問題（量測值不反映真實狀態）

---

## 六、關於「沒有保養記錄」的說明

本系統設計上**不強制依賴保養記錄**，原因是：

1. **自動基準期偵測**已能在大多數情況下找到合理的健康基準
2. 即使基準期不是「最佳狀態」，**趨勢分析**仍然有效（分數持續下降就是劣化訊號）
3. 保養記錄的主要用途是：
   - 驗證自動選定的基準期是否正確
   - 在報告圖上畫保養事件垂直線，方便視覺比對

**最終建議流程**：
1. 先跑 `test_baseline.py` 看自動偵測的基準期日期
2. 和現場人員口頭確認「那個時段設備有沒有剛保養過」
3. 若確認正確 → 直接使用
4. 若不正確 → 填入 `train_start/train_end` 後重跑
